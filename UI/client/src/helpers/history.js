/**
 * Module: history
 * Description: Creates a browser history object using the createBrowserHistory function from the 'history' library.
 *              This history object is used for navigation and managing browser history state in React applications.
 */
import { createBrowserHistory } from "history";

// Create a browser history object
export const history = createBrowserHistory();
